@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Información Personal";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="text-center p-2">
    <h4>@ViewData["Title"]</h4>
</div>


<div class="row d-flex justify-content-center">
    <div class="col-md-6 bg-white p-4 rounded shadow">
        <p>Su cuenta contiene datos personales que nos ha proporcionado. Esta página le permite descargar o eliminar esos datos.</p>
        <p>
            <strong>Eliminar estos datos eliminará permanentemente su cuenta y no se podrá recuperar.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group text-center">
            <button class="btn btn-primary" type="submit">Descargar</button>
        </form>
        <p class="text-center">
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-secondary">Eliminar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
